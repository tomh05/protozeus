<%= form_for([@card, @card.card_parameters.build]) do |f| %>
  <% if @card_parameter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@card_parameter.errors.count, "error") %> prohibited this card_parameter from being saved:</h2>

      <ul>
      <% @card_parameter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
