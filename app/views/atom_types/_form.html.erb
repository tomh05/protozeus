<%= form_for(@atom_type) do |f| %>
  <% if @atom_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@atom_type.errors.count, "error") %> prohibited this atom_type from being saved:</h2>

      <ul>
      <% @atom_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
